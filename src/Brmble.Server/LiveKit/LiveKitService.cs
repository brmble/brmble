namespace Brmble.Server.LiveKit;

public class LiveKitService
{
    // TODO: GenerateToken(string certHash, string roomName, TokenPermissions permissions) → string
    //   - Resolve user identity from certHash
    //   - Generate scoped LiveKit JWT
}
