query {
  repository(owner: "brmble", name: "brmble") {
    pullRequest(number: 511) {
      reviewThreads(first: 20) {
        nodes {
          id
          isResolved
          comments(first: 1) {
            nodes {
              databaseId
            }
          }
        }
      }
    }
  }
}
