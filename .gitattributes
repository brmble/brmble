# Default: normalize line endings to LF on checkout
* text=auto eol=lf

# Shell scripts must always be LF
*.sh text eol=lf

# Windows-only files
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
